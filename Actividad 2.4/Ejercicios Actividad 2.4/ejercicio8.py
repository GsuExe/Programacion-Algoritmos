factor1 = 1

while factor1 <= 10:
    print(f"\nTabla de multiplicar del {factor1}")
    factor2 = 1
    while factor2 <= 10:
        print(f"{factor1} * {factor2} = {factor1 * factor2}")
        factor2 += 1
    factor1 += 1
